# 복습 큐

복습은 각 개념 페이지의 `review_due`/`review`/`importance`로 구동된다(§CLAUDE.md 4).
이 파일은 사람이 빠르게 훑는 **요약 뷰**다 — 코치가 데일리 세션마다 갱신.

## 오늘 due (importance 내림차순)
_(2026-07-24 세션 처리 — due 3개, 예산 내 상위 2개 처리 + 1개 이월)_
- ✅ **클로저 m3→4 복귀**: `makeCounter` 무힌트 `1 2 1`+힙+수명=도달가능성 자력(7-22 오답 회복). 잔여 "함수마다 배낭" 표현 오개념을 `{inc,get}` 반례로 자가교정 → **배낭=호출 소유** 봉인. → **7-31**.
- ⚠️ **HTTP m3 유지**: 3층·4xx(클라)/5xx(서버)·fetch 404 resolve는 30일 공백에도 durable / **`Host` 존재 이유 회귀**("IP 하나=사이트 하나" 오개념) → 버추얼 호스팅 정정 + "왜 첫 줄 아닌 헤더=하위 호환" 새 질문 해소. → 30→14일 당김 **8-07**(다음에 "한 IP 여러 도메인→Host" 무힌트면 m4).
- 🥉 **쿠키/세션/토큰 미착수**(사용자 중단) → due **7-23 유지**, 다음 세션 최상위 이월(몰아치기 금지, 죄책감 없이).

> **2026-07-27 발행 세션 — 복습 0.** `/card` 발행 트랙만 돌린 운영 세션이라 큐를 열지 않음.
> 쿠키/세션/토큰은 **4일 밀림**(due 7-23). §4.1대로 벌하지 않되, **다음 세션은 복습부터** 시작할 것.
> (발행 트랙이 복습 예산을 먹지 않게 — 카드는 학습이 아니다.)

## 곧 due
**⭐다음 세션 최상위 — 쿠키/세션/토큰 (imp 4, m3, 이월·4일 밀림):**
- 앵커: stateless→쿠키(헤더에 티켓, `Set-Cookie`/자동 `Cookie`)→세션 vs 토큰("누가 상태 기억하나" 축)→트레이드오프 양 날(확장↔즉시 무효화).
- 🎯 응용 시험: **서버 3대 확장 시나리오**로 세션 곤란/토큰 편함(확장성) 자력 여부 + 탈취 시 즉시 무효화 약점.

**2026-08-05 — 프로세스/스레드 (imp 4, m3):** capstone("싱글스레드=race 면제 선물, 대가=병렬 포기") + 언어/엔진(V8)/호스트 3층 자력.

**2026-08-12 — frontend 비동기 클러스터 6개 (통합 앵커 1방):**
- 콜 스택 (imp 5, m3) · 이벤트 루프 (imp 5, m4) · Promise (imp 5, m3) · async/await (imp 4, m4) · micro (imp 4, m3) · macro (imp 4, m3).
- 🎯 앵커: 출력 순서 퍼즐 하나로 등록 순서·micro 싹/macro 1개·`await`=`.then` 감쌈까지.
- 🔴 **부채(6-24·7-22 미이행):** 통합 앵커 후 **imp5 트리오(콜스택·이벤트루프·Promise)는 개별로 콕** — 두 세션 연속 시간 부족으로 못 함. 이번엔 반드시.

**2026-08-21 — 🔴 Promise.all 병렬 vs 순차 (imp 4, m5·mastered):**
- **m5 유지 조건:** 봉인 문장을 `Web Worker` 이름까지 무힌트로 꺼낼 것 — "Promise.all은 기다림을, Web Worker는 일을 겹친다." 못 꺼내면 m4로 강등.

## 유지 복습 루프 (importance 5, 졸업 없음)
<!-- 90일 주기로 계속 도는 핵심 개념 -->
- 이벤트 루프 · 콜 스택 · 클로저 · Promise (importance 5 → 졸업 없음, 영구 유지)

## ✅ 메타 (폭탄일 → 정책으로 해결됨, 2026-07-22)
- 폭탄일 3연속(6-24·7-01·7-22)의 진짜 원인 = 공백이 아니라 **복귀 시 몰아치기.** (공백 자체는 SRS가 원함.)
- **해결:** CLAUDE.md **§4.1 세션 예산제** 신설(목표 주 2–3회) — due는 우선순위 큐, 세션당 상위 3–4 유닛만, 나머지는 죄책감 없이 이월, 공백 통과 시 간격 더 점프, 실패만 리셋. **"폭탄일" 개념 자체가 폐기됨.**
- 앞으로 며칠·몇 주를 비워도 코치는 상위 3–4개만 꺼낸다. 몰아치기 금지가 규칙.

## retired (복습 제외)
<!-- 사용자가 온전히 이해해 큐에서 뺀 것 -->
