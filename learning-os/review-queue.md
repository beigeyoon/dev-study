# 복습 큐

복습은 각 개념 페이지의 `review_due`/`review`/`importance`로 구동된다(§CLAUDE.md 4).
이 파일은 사람이 빠르게 훑는 **요약 뷰**다 — 코치가 데일리 세션마다 갱신.

## 오늘 due (importance 내림차순)
_(2026-07-01 세션 처리)_
- **복습 폭탄일 2회차: due 11개 전부** (6-24 이후 일주일, 클러스터 통째 만기).
- **처리:** 🔴 Promise.all 단독 Feynman만 진행 → **회귀 완전 회복(m3→4)**, review_due **7-08**.
- **나머지 10개: 미복습 이월** (사용자 "여기서 마감"). mastery 불변, review_due만 테마별 재배치(폭탄일 재발 방지):
  - → **7-02 (frontend 데이):** 통합 앵커 1방 + 클로저.
  - → **7-03 (network·os 데이).**

## 곧 due (7일 내)
**2026-07-02 — frontend 데이 (통합 앵커 1방으로 6개 동시 점검):**
- 콜 스택 (imp 5, m3) · 이벤트 루프 (imp 5, m4) — 유지 복습(졸업 없음)
- Promise (imp 5, m3) · async/await (imp 4, m3) · 마이크로태스크 큐 (imp 4, m3) · 매크로태스크 큐 (imp 4, m3)
- 🎯 앵커 후보: 출력 순서 퍼즐 하나로 등록 순서·micro 전부 비움/macro 1개·await 분할까지. **imp5(콜스택·이벤트루프·Promise)는 앵커 후 개별로 한 번씩 콕 찌를 것.**
- 클로저 (imp 5, m4) — 별개 메커니즘. 점검: makeCounter/makeBank로 "배낭=호출 소유"+수명=도달 가능성+힙 대피+GC 무힌트.

**2026-07-03 — network·os 데이:**
- HTTP (imp 4, m3) — 요청 3층·Host 1차 원리·fetch 404 함정 무힌트.
- 쿠키/세션/토큰 (imp 4, m3) — "누가 상태 기억하나" 축 + 트레이드오프 양 날(확장↔즉시 무효화). 이번엔 토큰 강점(확장성)이 자력으로?
- 프로세스/스레드 (imp 4, m3) — capstone("싱글스레드=race 면제 선물, 대가=병렬 포기") + 언어/엔진(V8)/호스트 3층 자력 재구성.

## 7-03 이후
- 🔴 Promise.all 병렬 vs 순차 (imp 4, m4) — **2026-07-08.** 점검: **Web Worker 전이("Promise.all은 기다림을, Worker는 일을 겹친다")를 무힌트로 꺼내면 m5.**

## 유지 복습 루프 (importance 5, 졸업 없음)
<!-- 90일 주기로 계속 도는 핵심 개념 -->
- 이벤트 루프 · 콜 스택 (importance 5 → 졸업 없음, 영구 유지)

## retired (복습 제외)
<!-- 사용자가 온전히 이해해 큐에서 뺀 것 -->
