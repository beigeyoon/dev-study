---
title: 클로저
type: concept
domain: frontend
knowledge_type: model
status: understood
mastery: 4
importance: 5
review: auto
feynman_passed: true
created: 2026-06-21
updated: 2026-07-31
sources: []
related: [concepts/call-stack.md, concepts/async-await.md, concepts/macrotask-queue.md, concepts/cookie-session-token.md, concepts/in-memory-vs-relational-db.md]
tags: [closure, scope, memory, heap, javascript, execution]
review_due: 2026-08-14
---

## 한 줄 정의
함수가 *태어난 곳*의 변수를 기억하고 그 변수를 살려둔 채 들고 다니는 것. 함수의 변수 접근은 *어디서 호출되는가*가 아니라 *어디서 정의(탄생)됐는가*로 결정된다(렉시컬 스코프). 클로저가 잡은 변수는 콜 스택 프레임이 pop돼도 **힙**에 남아 살아남는다.

## 깊은 설명
- **출발한 모순:** `setup(){ let message='안녕'; setTimeout(()=>console.log(message),1000) }` — `setup()`은 즉시 끝나 스택 프레임이 pop되는데, 1초 뒤 콜백은 `message`를 멀쩡히 읽는다. "함수 끝나면 지역 변수 사라진다"는 *기본값일 뿐* 절대 법칙이 아니라는 신호.
- **왜 안 죽나 = 도달 가능성:** 안쪽 콜백이 `message`를 참조하고, 그 콜백이 바깥 함수보다 오래 산다(큐로 넘어가 나중 실행). 콜백이 살아있는 한 `message`도 버릴 수 없다. **변수 수명 = 프레임 수명이 아니라 "누가 아직 참조하나".**
- **어디에 사나 = 힙:** 클로저가 잡은 변수 묶음(렉시컬 환경)은 **콜 스택이 아니라 힙**에 둔다. 스택 프레임은 `return` 시 pop되지만 힙의 묶음은 GC가 "도달 불가"로 판정할 때까지 산다. 큐에 있는 콜백은 그 힙 묶음을 **가리키는 참조**를 들고 있을 뿐(큐는 할 일 대기열이지 변수 저장소가 아니다).
- **스택 vs 힙:** 스택 = 짧게 사는 프레임(LIFO 자동 정리). 힙 = 오래 살아야 하는 것(GC가 도달 불가 시 회수). 클로저는 "지역 변수를 스택에서 힙으로 대피시키는" 장치로 볼 수 있다.
- **상태 유지(비동기 전용 아님):** `makeCounter(){ let count=0; return ()=>++count }` → 반환 함수가 `count`를 잡아 힙에 유지 → 호출마다 1,2,3. **하나의 `count`를 공유하는 살아있는 참조**(스냅샷 복사가 아니다). 함수가 사적이고 지속되는 상태를 갖게 하는 메커니즘.

## 내 파인만 설명 (직접 작성)
_(2026-06-21 세션, 콜 스택 미해결 질문에서 추론으로 도출)_

- 모순 앞에서 **"(a) 함수 끝나면 지역 변수 사라진다가 틀렸다"**를 직감으로 골라냄 → 한 칸 정밀화: (a)는 *보통은 맞고*, 이 케이스는 예외. 예외의 원인 = `setup` 안에서 태어난 콜백이 `message`를 참조 + 콜백이 더 오래 삶.
- "스택 프레임은 pop됐는데 `message`는 어디에?" → 큐로 한 번 빗나갔다가(큐=할 일 대기열이지 변수 저장소 아님) **힙**으로 교정. 콜 스택의 단짝.
- **응용 전이 성공:** `makeCounter`에서 "makeCounter는 곧장 pop돼도 반환 함수가 `count`를 참조하므로 `count`는 힙에 남아 계속 참조된다 → 1,2,3"을 자력 설명. 비동기(setTimeout) 사례에서 동기(상태 유지) 사례로 **같은 원리를 옮김.**
- **클로저의 형태(질문에서 도출):** 클로저는 코드로 쥐는 독립 객체/값이 *아니다* — `console.log(c)`는 *함수*만 찍히고(`typeof c === "function"`), 잡힌 변수는 안 보인다. 함수의 숨은 내부 슬롯 `[[Environment]]`가 힙의 환경 레코드를 가리킬 뿐. 코드로는 못 읽고(=사적, 이게 캡슐화의 핵심), Chrome DevTools `[[Scopes]] > Closure`로만 엿본다. → **"특정 가능한 건 함수, 배낭은 실재하지만 사적."**
- **배낭은 함수가 아니라 스코프(호출) 소유:** 한 호출 안에서 태어난 함수들은 *같은* 환경을 공유(`inc`/`dec`가 한 `count`를 나눠 1,2,1). 호출마다 새 환경 → 독립.
- **var/let 함정 자력 해결:** `for(var)` + setTimeout 3개 → `3,3,3`. ① 반복문(동기)이 끝까지 돌아 공유 `i`가 3으로 굳은 뒤 ② 콜백이 그 하나의 `i`를 늦게 읽음(스냅샷 ❌). `for(let)` → `0,1,2`: 반복마다 *새 바인딩*이라 콜백마다 자기 `i`(0/1/2)에 박제. **두 버전 다 클로저** — 차이는 "배낭 1개 공유 vs 3개 분리".

### 2026-06-22 복습 (첫 간격, mastery 3→4) — "배낭은 누구 소유인가" 새 앵글
앵커: `makeBank(balance){ return { deposit:n=>balance+=n, check:()=>balance } }` → `a=makeBank(100); b=makeBank(0); a.deposit(50); log(a.check(), b.check())` → **150, 0**.
- **무힌트 재구성:** ① 한 호출 안의 `deposit`·check`는 *같은* 배낭(같은 `balance` 참조) 공유 → deposit이 누른 걸 check가 봄. ② `b`는 `makeBank`의 *다른 호출* → 새 배낭 → 안 건드려짐. **배낭 = 함수 아닌 호출(스코프) 소유** 재확인.
- **밑단까지 팜(1차 원리):** `makeBank` 프레임이 `return` 직후 pop돼도 `balance`가 안 죽는 이유 = **deposit/check가 아직 참조 → 도달 가능 → 수명은 프레임이 아니라 참조**. 거처 = **힙**(스택 아님). 전이: `a=null`로 참조 끊으면 → 도달 불가 → **GC가 힙에서도 회수**. 클로저 트레이드오프(상태 캡처 편의 ↔ 안 끊으면 메모리 누수)의 뒷면을 자력으로 짚음.

### 2026-07-22 복습 (⚠️ 회귀 → 유도 회복, mastery 4→3, 1일 리셋)
3주 공백(7-01→7-22) 뒤 `makeCounter` 앵커에서 **회귀 노출** — imp5 개념도 공백 앞에선 무너진다는 증거:
- **출력을 `1 2 3`으로 오답**(정답 `1 2 1`) + **"a와 b가 count를 공유"라는 핵심 오개념**을 들고 있었음. 6-22에 굳었던 "배낭=호출 소유"가 3주 만에 흐려짐.
- **유도 회복:** `let count=0`이 *어디/언제* 실행되나 힌트 → "함수 **안**, **호출마다** 실행 → `makeCounter()` 2번 호출 → count 변수 2개"를 스스로 재도출 → a·b가 서로 다른 배낭이라 출력은 `1 2 1`, 공유 아님으로 교정.
- **프레임 pop 후 생존(핵심):** "배낭은 콜스택이 아니라 **heap**에 따로 저장 / 엔진은 **참조하는 곳이 하나도 없을 때만** 회수"로 수명=도달 가능성 재확립.
- 회복이 *자력*이 아니라 *유도*라 m4 아님 → **m4→3, review_due 1일 리셋(2026-07-23).** 내일 같은 앵커를 무힌트로 넘기면 m4 복귀.

### 2026-07-24 복습 (✅ 무힌트 통과 → mastery 3→4 복귀)
1일 리셋 뒤 `makeCounter` 앵커를 **무힌트로 재구성**:
- 출력 `1 2 1` 자력 + "a·b는 다른 호출이라 배낭 따로" + "**힙**에 살고, **참조가 하나라도 남으면** 산다"(수명=도달 가능성)까지 유도 없이. 7-22의 `1 2 3`/공유 오답 완전 회복.
- **정밀화(호출 소유 조임):** 답을 "**함수마다** 배낭 따로"로 *표현*하는 잔여 오개념을 반례로 사냥 — `한 호출 → { inc, get }` 두 함수가 `count`를 공유(`c.inc();c.inc();c.get()===2`). 사용자가 "함수마다였으면 inc/get이 갈라져야 하는데 안 갈라진다"는 모순을 **스스로 잡아** → **배낭 = 함수 아닌 호출(스코프) 소유**로 봉인. 6-22의 축이 이번엔 반례로 굳음.
- 무힌트 통과 + 표현까지 조여 **m4 복귀, review_due 7-31**(1일 리셋 후 첫 통과 → 계단 복귀).

### 2026-07-31 복습 (m4 유지) — 양방향 앵커 + "클로저는 무엇을 팔았나"
앵커: `makeCounters()`가 `{ inc, get }`을 반환 → `a`, `b` 두 호출 → `a.inc(); a.inc(); b.inc();` → 출력 **`2 1`**.
- **무힌트 3연속:** ① `a`·`b`는 **다른 배낭**이라 안 섞임 ② 한 호출 안의 `inc`/`get`은 **같은 배낭 공유**라 섞임 ③ `count`는 **힙**에 있고 *"아직 참조하는 곳이 남아있기 때문에"* 안 죽음. **수명 = 도달 가능성**을 자력으로 발화. 7-22 회귀는 완전히 봉인됨.
- **표현 정밀화:** *"a 함수"* → `a`는 함수가 아니라 반환된 **객체**. 정확한 규칙은 **배낭을 만드는 건 함수의 *정의*가 아니라 *호출*** — `makeCounters`는 함수 하나지만 호출이 둘이라 배낭이 둘. ①②가 이 한 규칙에서 동시에 떨어진다(7-24의 반례 봉인이 다른 앵커에서도 성립).
- **🎯 전이 시도(m5 관문 — 유도로만 도달):** 같은 날 [인메모리 vs 관계형 DB](in-memory-vs-relational-db.md)에서 세운 렌즈 *"공짜로 얻은 줄은 없다"* 를 클로저에 댐. "얻은 것/판 것" 표를 깔아준 뒤 **판 것 = 힙 메모리**를 회수. 표 없이 자력 전이는 아니었으므로 **m4 유지.**
  > **클로저 = 상태 유지·캡슐화를 사려고 "메모리의 자동 회수"를 판 거래.**
  > 스택의 지역 변수는 함수가 끝나면 **공짜로, 확실하게** 사라진다(LIFO). 클로저는 그걸 힙으로 대피시켜 살리는 대신, 그 메모리가 **"아무도 참조 안 할 때"까지** 안 없어진다. 즉 *언제 사라지는지 확신할 수 있음*을 팔아 *오래 살아있음*을 샀다.
  > 청구서가 실제로 청구되는 순간 = **메모리 누수**(리스너·`setInterval` 안 떼면 배낭 통째로 힙에 남음 → React `useEffect` cleanup 누락 버그의 정체).

## 연결 / 철학적 질문
- **resolves:** [콜 스택](call-stack.md)의 미해결 질문("프레임 사라진 뒤 콜백이 변수에 접근하는 법") — 답: 잡힌 변수는 힙으로 대피해 프레임과 따로 산다.
- **builds-on:** 콜 스택 / 힙 메모리 모델. **대조:** 스택(자동 pop) ↔ 힙(GC). **연결:** [async/await](async-await.md)·[매크로태스크 큐](macrotask-queue.md)의 콜백이 변수를 잡는 것도 전부 클로저.
- **why 중요:** 모듈 패턴(사적 변수), React `useState`/이벤트 핸들러가 렌더 시점 값을 기억, 디바운스/스로틀, `once`/메모이제이션 — 전부 클로저.
- **트레이드오프:** 안 풀리는 참조 = 메모리 누수 위험(콜백/리스너 안 떼면 힙이 안 비워짐). 편의(상태 캡처)와 메모리의 교환.
- **전이(2026-07-27):** "변수를 스택에 둘까 힙에 둘까"는 [쿠키/세션/토큰](cookie-session-token.md)의 "로그인 상태를 서버 안·공용 저장소·클라이언트 중 어디에 둘까"와 **같은 축**이다 — 상태의 위치를 고르면 거기 맞는 청구서(수명·비용·회수 가능성)가 따라온다. 언어 런타임과 분산 시스템이 같은 문제를 푼다.
- **전이 확장(2026-07-29) — 같은 저울의 세 번째 층위:** [인메모리 vs 관계형 DB](in-memory-vs-relational-db.md)가 세 번째 칸을 채웠다.

  | 층위 | 빠르고 · 짧게 살고 · 좁다 | 느리고 · 오래 살고 · 넓다 |
  |---|---|---|
  | **클로저**(여기) | 콜 스택 | 힙 |
  | 쿠키/세션/토큰 | 웹서버 안 메모리 | 공용 저장소 |
  | 인메모리 vs 관계형 | RAM (Redis) | 디스크 (MySQL) |

  세 번 다 답이 "빠른 쪽으로 몰아라"가 아니라 **"수명과 접근 빈도를 보고 배치하라"**였다.

- **전이 심화(2026-07-31) — 렌즈가 층을 건넜다:** database에서 세운 1차 원리 **"속도는 성질이 아니라 거래다 / 진짜 질문은 *무엇을 팔아도 되는가*"** 가 언어 런타임에도 그대로 먹힌다. Redis·인덱스·캐시·**클로저** 네 줄이 전부 같은 모양(얻은 것 ↔ 판 것)이고 공짜로 얻은 줄이 없다. → **"무엇을 팔았나"는 저장소 고를 때만 쓰는 질문이 아니라 모든 설계 결정에 대는 자.**

## 미해결 질문
- "렉시컬 환경 / 실행 컨텍스트 / 스코프 체인"의 정확한 구조 — 변수 탐색이 체인을 어떻게 거슬러 오르나? (→ 다음 학습 후보)
- ✅ 해소(2026-06-21): 루프 + `var` vs `let` 클로저 함정 → `var`=배낭 1개를 제자리 수정(콜백들이 최종값 3을 늦게 읽음 = `3,3,3`), `let`=반복마다 새 바인딩(각자 0/1/2 박제 = `0,1,2`). 둘 다 클로저, 차이는 배낭 개수. (위 "내 파인만 설명" 참고.)
- GC는 "도달 불가"를 정확히 어떻게 판정하나(mark-and-sweep)?

## 실전 사례
<!-- React 훅/이벤트 핸들러의 stale closure 버그, 모듈 사적 상태 등 실제 사례 연결. -->
